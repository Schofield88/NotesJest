const List = require('../src/list');

describe('List', () => {
  const list = new List();

  it('returns the array of notes', () => {
    expect(list.notes()).toEqual([]);
  });

  it('adds a new note', () => {
    expect().toBe();
  });
});
